import { ProtectedPlaceholder } from "@/components/ProtectedPlaceholder";

export default function CasesPage() {
  return (
    <ProtectedPlaceholder
      eyebrow="// Case Workspace"
      title="Cases"
      description="Case files will organize evidence, reports, collaborators, legal status, and review activity around durable investigation records."
    />
  );
}
